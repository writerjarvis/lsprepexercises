/* What does the following function do?

function doSomething(string) {
	return string.split(' ').reverse().map((value) => value.length);
}

Answer below:

Code converts a string into an array of words, reverses that array, then returns a new array that contains the lengths of the words. */