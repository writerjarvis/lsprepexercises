/* What does the code log to the console? Why?

let array1 = [1, 2, 3];
let array2 = array1;
array1[1] = 4;
console.log(array2);

answer below:

It will log [1, 4, 3], on the 3rd line we change array1 and remove the 4 and replace it with a 2. */